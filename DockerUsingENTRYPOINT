# from base image node
FROM node:14

CMD ["-v"]
ENTRYPOINT ["node"]

#build the image
# docker build -t entrypoint-dockerfile -f DockerUsingENTRYPOINT . 

#run the image
#docker run -it -d entrypoint-dockerfile
